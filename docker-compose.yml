services:
  whatsapp-bridge:
    build:
      context: .
      dockerfile: Dockerfile
    image: whatsapp-bridge:latest
    container_name: whatsapp-bridge
    restart: unless-stopped

    ports:
      # Dışarıya açık port — reverse proxy (nginx/traefik) arkasında kullanımı önerilir
      - "${WA_HOST_PORT:-9277}:9277"

    environment:
      WA_PORT:        "9277"
      WA_API_KEY:     "${WA_API_KEY}"
      WA_AUTH_DIR:    "/data/auth"
      WA_MAX_RETRIES: "${WA_MAX_RETRIES:-10}"
      LOG_LEVEL:      "${LOG_LEVEL:-info}"

    volumes:
      # WhatsApp oturum dosyaları — container yeniden başlatıldığında QR tekrar taranmaz
      - wa_auth:/data/auth

    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:9277/status"]
      interval: 30s
      timeout:  5s
      retries:  3
      start_period: 15s

volumes:
  wa_auth:
    driver: local
